{{ form_start(form) }}
<br>
    {{ form_widget(form) }}
    <br>

    <div class="flex">
        <div><button class="btn btn-primary btn-block">{{ button_label|default('ENREGISTRER') }}</button></div>

{{ form_end(form) }}
